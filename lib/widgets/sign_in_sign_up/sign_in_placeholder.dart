import 'package:flutter/material.dart';

class SignInPlaceholder extends StatelessWidget {
  const SignInPlaceholder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
